# gRPC 基础
grpcio==1.60.0
grpcio-tools==1.60.0

# AI 核心 (M1 Mac 会自动安装适配 ARM 的 torch)
torch
transformers
pillow
requests

# OCR (飞桨)
paddlepaddle
paddleocr
opencv-python-headless

# Qwen2-VL 依赖
qwen-vl-utils
accelerate>=0.26.0
torchvision